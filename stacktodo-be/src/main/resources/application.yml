spring:
  datasource:
    url: jdbc:mysql://localhost:3306/stacktodo?useSSL=false&allowPublicKeyRetrieval=true&characterEncoding=utf8&serverTimezone=Asia/Seoul
    username: stackuser
    password: 1234!
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
    open-in-view: false
    database: mysql   # ?? ?? ??
    database-platform: org.hibernate.dialect.MySQL8Dialect # ?? ?? ??

server:
  port: 8080